# Copy, fill in, apply, and do NOT commit the filled file:
#   kubectl create namespace lmbot
#   kubectl -n lmbot create secret generic lmbot-secrets \
#     --from-literal=sa-password='Chang3-me-Str0ng!'
apiVersion: v1
kind: Secret
metadata:
  name: lmbot-secrets
  namespace: lmbot
type: Opaque
stringData:
  sa-password: "Chang3-me-Str0ng!"     # 12+ characters with upper, lower, digit and symbol
