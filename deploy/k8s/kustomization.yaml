apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: lmbot
resources:
  - sqlserver.yaml
  - bot.yaml
  - networkpolicy.yaml
# The secret is not in the repository: create it first (docs/deployment.md).
